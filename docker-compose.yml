services:
  music-lib:
    build: .
    image: skorotkiewicz/music-lib:latest
    container_name: music-lib
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./hls_cache:/app/hls_cache
    environment:
      # Set to "true" for readonly mode
      - READONLY=false
